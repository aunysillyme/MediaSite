{
  "name": "auny-media",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm run build:site && vite",
    "build": "npm run build:site && vite build",
    "build:site": "npm run build:images && npm run build:singles && npm run build:albums && npm run build:home && npm run build:poetry && npm run build:llms && npm run build:sitemap",
    "build:singles": "node scripts/build-singles.mjs",
    "build:albums": "node scripts/build-albums.mjs",
    "build:home": "node scripts/build-home.mjs",
    "build:sitemap": "node scripts/build-sitemap.mjs",
    "preview": "vite preview",
    "build:images": "node scripts/optimize-images.mjs",
    "build:poetry": "node scripts/build-poetry.mjs",
    "build:llms": "node scripts/build-llms.mjs"
  },
  "devDependencies": {
    "sharp": "^0.35.0",
    "vite": "^6.0.7"
  }
}